<% if (!partial) { %>
<%- include('../layouts/header'); %>
<% } %>
    <div class="main-content">
        <div class="topbar">
            <div>
                <h5 class="mb-1">Task Details</h5>
                <small class="text-muted">Review task</small>
            </div>
            <a href="/tasks" class="btn btn-secondary" data-spa>
                <i class="bi bi-arrow-left"></i> Back to Tasks
            </a>
        </div>

        <div class="container-fluid mt-4">
            <div class="card">
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <h6 class="text-muted mb-1">Title</h6>
                            <div class="fw-semibold">Prepare monthly report</div>
                        </div>
                        <div class="col-md-3">
                            <h6 class="text-muted mb-1">Assignee</h6>
                            <div class="fw-semibold">Alex Kim</div>
                        </div>
                        <div class="col-md-3">
                            <h6 class="text-muted mb-1">Status</h6>
                            <span class="badge bg-warning text-dark">In Progress</span>
                        </div>
                        <div class="col-12">
                            <h6 class="text-muted mb-1">Description</h6>
                            <p class="mb-0">Compile KPIs and financial metrics for the leadership review.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

<% if (!partial) { %>
<%- include('../layouts/footer'); %>
<% } %>
